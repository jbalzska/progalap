#include <stdio.h>
#include <stdlib.h>

int main()
{
    /*

    Két egész szám felcserélése függvény használatával.

    */

    /*int a = 10;
    int b = 5;
    int c = 0;
    c = a;
    a = b;
    b = c;
    printf("a: %d | b: %d | c: %d", a, b, c);*/

    int a = 0, b = 0;
    printf("Kerem az (a, b) erteket! ");
    scanf()
    csere(10, 5); //a=x=10 | b=y=5

    return 0;
}
void csere(int x, int y)
{
    int c;
    c = x;
    x = y;
    y = c;

    printf("a: %d | b: %d", x, y); //Felcserélt
}
